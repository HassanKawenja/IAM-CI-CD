name: Deploy IAM Role

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

# This environment variable forces the runner to use Node 24 for the actions
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::767397679075:role/GitHubAssumeRole
          aws-region: us-east-1

      - name: Deploy CloudFormation
        run: |
          aws cloudformation deploy \
            --template-file infrastructure/iam-role.yaml \
            --stack-name iam-cicd-stack \
            --capabilities CAPABILITY_NAMED_IAM